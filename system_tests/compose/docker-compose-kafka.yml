
version: '2'

services:
  kafka:
    image: vectorized/redpanda:latest
    hostname: kafka
    ports:
      - "9092:9092"

  ioc:
    image: screamingudder/softioc:69433bc
    stdin_open: true
    tty: true
    network_mode: "host"

  rpk_admin:
    image: vectorized/redpanda:latest
    stdin_open: true
    tty: true
    network_mode: "host"
    entrypoint: bash
